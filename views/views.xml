<odoo>
  <data>
    <!-- explicit list view definition -->
    <!--
    <record model="ir.ui.view" id="report_generate.list">
      <field name="name">report_generate list</field>
      <field name="model">report_generate.report_generate</field>
      <field name="arch" type="xml">
        <tree>
          <field name="name"/>
          <field name="value"/>
          <field name="value2"/>
        </tree>
      </field>
    </record>
    -->

    <!-- actions opening views on models -->
    <!--
    <record model="ir.actions.act_window" id="report_generate.action_window">
      <field name="name">report_generate window</field>
      <field name="res_model">report_generate.report_generate</field>
      <field name="view_mode">tree,form</field>
    </record>
    -->

    <!-- server action to the one above -->
    <!--
    <record model="ir.actions.server" id="report_generate.action_server">
      <field name="name">report_generate server</field>
      <field name="model_id" ref="model_report_generate_report_generate"/>
      <field name="state">code</field>
      <field name="code">
        action = {
          "type": "ir.actions.act_window",
          "view_mode": "tree,form",
          "res_model": self._name,
        }
      </field>
    </record>
    -->

    <!-- Top menu item -->
    <!--
    <menuitem name="report_generate" id="report_generate.menu_root"/>
    -->
    <!-- menu categories -->
    <!--
    <menuitem name="Menu 1" id="report_generate.menu_1" parent="report_generate.menu_root"/>
    <menuitem name="Menu 2" id="report_generate.menu_2" parent="report_generate.menu_root"/>
    -->
    <!-- actions -->
    <!--
    <menuitem name="List" id="report_generate.menu_1_list" parent="report_generate.menu_1"
              action="report_generate.action_window"/>
    <menuitem name="Server to list" id="report_generate" parent="report_generate.menu_2"
              action="report_generate.action_server"/>
    -->
  </data>
</odoo>